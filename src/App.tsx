import "./App.css";

function App(): JSX.Element {
	return (
		<div className="app">
			<h1>Electron + React + TypeScript</h1>
			<p>Your default starter app is ready.</p>
		</div>
	);
}

export default App;
